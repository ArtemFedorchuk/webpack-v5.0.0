name: Test Webpack App

on: push
jobs:
  runs-on: ubuntu-latest
  steps:
    - name: Get a repository code
      uses: actions/checkout@v3
    - name: Install dependencies
      run: npm ci
    - name: Test App
      run: npm run dev
